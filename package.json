{
  "name": "gunny-chibi",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server/server.js",
    "start": "node server/server.js",
    "test": "node --test",
    "check": "find src server -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test:browser": "node scripts/browser-smoke.cjs",
    "test:load": "node scripts/load-smoke.cjs"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "playwright": "^1.55.0"
  }
}
